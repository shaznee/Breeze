<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".activities.MainActivity"
    android:background="@drawable/bg_gradient">

    <android.support.v4.view.ViewPager
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MORE"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:id="@+id/moreLabel"
        android:textColor="@android:color/white"
        android:textSize="@dimen/menu_text_size"
        android:onClick="onMoreLabelClick"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ADD"
        android:id="@+id/addLabel"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/moreLabel"
        android:layout_toStartOf="@+id/moreLabel"
        android:paddingRight="@dimen/menu_padding"
        android:textColor="@android:color/white"
        android:textSize="@dimen/menu_text_size"
        android:onClick="onAddLabelClick"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CITIES"
        android:id="@+id/citiesLabel"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/addLabel"
        android:layout_toStartOf="@+id/addLabel"
        android:paddingRight="8dp"
        android:textColor="@android:color/white"
        android:textSize="@dimen/menu_text_size"
        android:onClick="onCitiesLabelClick"/>


</RelativeLayout>